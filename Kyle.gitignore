# 1. Update .gitignore
cat > .gitignore << 'EOF'
.streamlit/secrets.toml
config_local.py
pass.env
.env
__pycache__/
*.pyc
.idea/
EOF

# 2. Create secrets directory
mkdir -p .streamlit

# 3. Create secrets file (DO NOT commit this!)
cat > .streamlit/secrets.toml << 'EOF'
DB_HOST = "mysql-11beff9b-kamarwatson36-874b.g.aivencloud.com"
DB_PORT = 11510
DB_USER = "avnadmin"
DB_PASSWORD = "AVNS_Dxyg2mu3MEiRoVyasff"
DB_NAME = "defaultdb"

EMAIL_USER = "fintrackeralerts@gmail.com"
EMAIL_PASSWORD = "myhdkbyrzmpvwjyb"
EOF

# 4. Verify secrets.toml is NOT tracked
git status
# Should NOT show secrets.toml
